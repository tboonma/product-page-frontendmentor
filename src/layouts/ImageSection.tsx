import React from 'react'
import { IImageSection } from '../models/IImageSection'
import { ImageSectionProps } from '../models/ImageSectionProps'

const ImageSection = (props: ImageSectionProps) => {
    return <div></div>
}

export default ImageSection
